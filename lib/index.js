'use strict';

const {createComponent, serveComponent, test} = require('./component');
const {serveApp, createApp, createComponentInApp} = require('./pwa');

module.exports = {createComponent, serveComponent, test, serveApp, createApp, createComponentInApp};
