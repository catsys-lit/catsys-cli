'use strict';

const {createComponent, serveComponent, test} = require('./component');
const {serveApp, createApp} = require('./pwa');

module.exports = {createComponent, serveComponent, test, serveApp, createApp};
