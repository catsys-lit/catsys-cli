'use strict';

const {createComponent} = require('./create');
const {serveComponent} = require('./serve');
const {test} = require('./test');

module.exports = {createComponent, serveComponent, test};
